Por favor, refatore o componente `<Experience />` aplicando o Princípio de Responsabilidade Única (SRP) do SOLID, separando os dados estáticos da camada de visualização.

Siga estes passos rigorosamente:

1. Crie um novo diretório chamado `data` na raiz do projeto (ou dentro de `src`, acompanhando a configuração do seu path alias `@/`).
2. Dentro desta pasta, crie um arquivo chamado `experiences.ts`.
3. Defina e exporte uma interface/type em TypeScript para a estrutura de dados (ex: `ExperienceType`).
4. Mova o array `experiences` do arquivo do componente para este novo arquivo `experiences.ts` e exporte-o.
5. No componente `Experience.tsx`, importe o array utilizando o alias `@/data/experiences`.
6. Mantenha toda a funcionalidade, design e comportamento originais do componente (incluindo as regras de acessibilidade e classes do Tailwind).
7. Todo o código gerado (variáveis, tipos, interfaces) deve estar estritamente em inglês, sem absolutamente nenhum comentário. O conteúdo de texto dos dados e da UI permanece em português.
