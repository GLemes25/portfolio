Por favor, aplique o princípio DRY (Don't Repeat Yourself) centralizando os dados pessoais do portfólio. Siga os passos rigorosamente:
1. use o arquivo `profile.ts` dentro do diretório `data`.
2. Escaneie todos os componentes da interface (especialmente `<Hero />`, `<Footer />` e `<Contact />` se existirem) que contenham esses dados fixos.
3. Substitua as strings estáticas pela importação do `profileData`. Por exemplo:
   - Substitua o href do email por `mailto:${profileData.email}`
   - Substitua a string "Gabriel Lemes" por `{profileData.shortName}`
   - Atualize os links do GitHub, LinkedIn e WhatsApp utilizando os caminhos do objeto.
4. Regra absoluta: Mantenha a lógica visual e a regra de acessibilidade intactas. Todo o código gerado deve estar em inglês, sem absolutamente nenhum comentário. O texto da UI não mapeado no objeto deve continuar em português do Brasil.
