refatore o componente @components/Hero.tsx atual aplicando as seguintes melhorias de UI/UX, acessibilidade e performance:

1. Responsividade da Imagem: Remova o `hidden md:block` da imagem. No mobile, a imagem deve aparecer no topo (acima do texto) de forma centralizada e menor (talvez um avatar circular ou com bordas arredondadas e glow sutil), e no desktop (telas md ou maiores) ela volta para a estrutura de grid atual de duas colunas.
2. Performance LCP: Adicione a propriedade `priority` no componente `<Image />` do Next.js, pois é uma imagem acima da dobra.
3. Acessibilidade: Adicione atributos `aria-label` corretos para todos os links de ícones sociais (Github, LinkedIn, Email).
4. Correção de Links: Atualize o href do ícone de e-mail para "mailto:gabriellemes924@gmail.com".
5. Estado de Foco: Em todos os botões e links sociais, adicione classes de `focus-visible` que apliquem 90% de opacidade (`focus-visible:opacity-90`) e removam outlines nativos agressivos (`focus-visible:outline-none`), garantindo uma navegação por teclado elegante.
6. Regras de Código: O código inteiro (variáveis, funções, propriedades) deve ser escrito estritamente em inglês, sem absolutamente nenhum comentário. O texto visível para o usuário final na interface deve permanecer em português do Brasil.

utilize smpre as regras em .claude/rules
